# ACT Dataset Collection Guideline — myCobot 280 Pick & Place

> 이 가이드는 `phase3_collect.py` 실행 전 반드시 숙지하고 환경을 세팅할 것.  
> ACT는 시연 품질에 매우 민감 — 나쁜 데이터 50개보다 좋은 데이터 50개가 낫다.

---

## 1. 수집 목표 요약

| 항목 | 값 |
|------|-----|
| 에피소드 수 | **100개** (최소 50개, from-scratch 학습이라 많을수록 좋음) |
| 에피소드 길이 | **5–12초** (짧으면 청크 학습 불가, 길면 노이즈 증가) |
| 수집 Hz | 30 Hz (스크립트 기본값) |
| 해상도 | 640 × 480 RGB |
| 카메라 위치 | **Top-down 고정** |
| 수집 방식 | **Leader-Follower** (리더팔 손으로 조종 → 팔로워팔 미러링) |

---

## 2. 환경 세팅

### 물리적 배치

```
[리더팔]          [팔로워팔 + 카메라]
사람이 조종         카메라가 촬영
카메라 밖           작업 공간 중앙
/dev/ttyUSB0        /dev/ttyUSB1
```

- **리더팔**은 카메라 프레임 밖에 위치 — 사람 손이 촬영되지 않음
- **팔로워팔**은 카메라 정면에 위치 — 실제 pick & place 수행

### 카메라

- **Top-down** 으로 팔로워 작업 공간 전체가 보이도록 설치 — 팔로워 로봇팔, 출발 위치, 도착 위치가 모두 프레임 안에 있어야 함
- 카메라는 **에피소드 중 절대 움직이지 않음** — 고정 거치대 또는 클램프 사용
- 에피소드 사이에도 카메라 위치 변경 금지 — 100개 전 에피소드 동일한 앵글이어야 함
- 조명은 **일정하게 유지** — 창문 햇빛처럼 시간에 따라 바뀌는 광원 피하기, 형광등/LED 고정 조명 권장
- 카메라와 테이블 사이 **그림자 없도록** 조명 방향 조정

### 작업 공간 (테이블)

- 배경색은 **단색, 무지, 밝은 색** 권장 — 흰색/회색 테이블보 사용 시 물체가 잘 분리됨
- 물체(블록 등)는 배경과 **명확히 구분되는 색** 사용 — 예: 빨간 블록 + 흰 테이블
- **출발 위치(A)** 와 **도착 위치(B)** 를 테이프로 바닥에 표시해두기 — 에피소드마다 리셋 기준점
- 작업 공간 외부 물체(노트북, 케이블 등)는 프레임 밖으로 치우기

### 홈 포지션

- 매 에피소드 시작 전 **리더팔 + 팔로워팔 모두** `[0, 0, 0, 0, 0, 0]` 에서 출발
- 스크립트가 자동으로 두 팔 홈 복귀시키지만, 실제로 복귀됐는지 눈으로 확인 후 시연 시작

---

## 3. 시연 방법 (Leader-Follower)

### 절차

```
1. 물체를 팔로워 작업공간의 출발 위치(A)에 놓는다
2. 리더팔 + 팔로워팔 모두 홈 포지션 확인
3. Enter 누름
     → 리더 모터 OFF (손으로 자유롭게 움직임)
     → 팔로워 모터 ON + 기록 시작
4. 리더팔을 손으로 잡고 시연
     - 리더팔을 A 위치에 대응하는 자세로 이동
     - 그리퍼로 집는 동작 수행
     - B 위치로 이동
     - 물체 내려놓기
5. Enter 누름 → 기록 종료 + 두 팔 홈 복귀
```

> **핵심:** 카메라는 팔로워만 촬영 — 리더팔과 사람 손은 프레임 밖에 있어야 함

### 그리퍼 주의사항

- 현재 스크립트는 **관절 6개만 기록**, 그리퍼 상태(open/close)는 별도 기록 안 됨
- 그리퍼를 쓰려면 `phase3_collect.py` 에 `mc.get_gripper_value()` 기록 추가 필요

---

## 4. Leader-Follower — 셋업 및 주의사항

### Leader-Follower란?

리더팔의 모터를 OFF 시켜 사람이 자유롭게 잡고 움직이면, 팔로워팔이 동일한 관절 각도를 실시간으로 따라가는 방식. 카메라는 팔로워 쪽만 촬영하므로 사람 손이 데이터에 등장하지 않음.

```
리더 모터 OFF → 손으로 리더팔 조종
팔로워 모터 ON → 리더 각도 30Hz로 미러링
카메라 → 팔로워 작업공간만 촬영
```

### 속도(MIRROR_SPEED) 튜닝

- 기본값: `80` (0–100)
- 너무 낮으면: 팔로워가 리더를 따라오지 못해 lag 발생 → 데이터 불일치
- 너무 높으면: 팔로워가 급격히 움직여 진동/오버슈트 발생
- 처음 테스트 시 `50`으로 시작해서 올려가며 조정

### 손이 카메라에 잡힐 경우

리더팔이 카메라 프레임 밖에 있으면 사람 손은 아예 안 보임. 만약 보인다면:
- 리더팔 위치를 카메라 반대편으로 이동
- 카메라 앵글을 팔로워 쪽으로 조정

---

## 5. 시연 시 반드시 지켜야 할 것

### 해야 할 것

- **천천히, 부드럽게** 움직인다 — ACT는 시연의 평균을 학습하므로 급격한 움직임은 노이즈
- **완전한 동작** 을 한 번에 보여준다 — 집기 → 이동 → 내려놓기 전 과정 포함
- 매 에피소드 **같은 속도, 같은 경로** 로 시연하는 것이 이상적
- 시연 전 **물체 위치를 A 지점으로 리셋** 한다

### 하면 안 되는 것

- **실패 시연 포함 금지** — 집다가 떨어뜨린 에피소드, 물체 못 집은 에피소드는 즉시 삭제
- **시연 중 멈춤 금지** — 중간에 생각하거나 쉬는 구간 없이 연속 동작
- **카메라 앞 손 가리기 금지** — 시연하는 손이 카메라 시야를 막으면 안 됨
- **에피소드 길이 3초 미만 금지** — ACT chunk size(~3.3초)보다 짧으면 학습 불가
- **에피소드 길이 15초 초과 주의** — 불필요하게 길면 노이즈 프레임 증가

---

## 6. 데이터 다양성 vs 일관성

| 항목 | 권장 |
|------|------|
| 물체 출발 위치 | A 지점 ± 2–3cm 범위에서 **조금씩 변화** — 완전히 동일하면 과적합 위험 |
| 물체 도착 위치 | B 지점으로 일관되게 |
| 집는 방향 (그리퍼 각도) | 일관되게 유지 |
| 팔 이동 경로 | 매번 거의 동일하게 |
| 조명 | 일관되게 |
| 물체 색/모양 | 100 에피소드 모두 **동일한 물체** 사용 |

> 다양성은 나중에 에피소드가 충분히 쌓인 뒤 추가. 처음부터 물체를 바꾸거나 위치를 크게 바꾸면 학습이 수렴하지 않음.

---

## 7. ACT 아키텍처 특성상 주의사항

- **Action Chunking**: ACT는 현재 프레임에서 **앞으로 ~3.3초(100 step × 30Hz)** 의 관절값을 한 번에 예측함 → 에피소드가 5초 이상이어야 충분한 청크 학습 가능
- **이미지가 핵심 입력**: 카메라가 흔들리거나 가려지면 모델이 전혀 작동 안 함 → 카메라 고정 최우선
- **Behavior Cloning 기반**: 모델은 시연을 그대로 모방 → 시연이 일관될수록 학습이 빠름
- **관절 각도 단위**: 스크립트는 pymycobot의 `get_angles()` 반환값(degree)을 그대로 저장 — 학습 전 stats.json에서 정규화되므로 별도 변환 불필요

---

## 8. 에피소드 품질 체크리스트

에피소드 종료 후, 저장하기 전 아래 항목 확인:

- [ ] 물체를 성공적으로 집었는가?
- [ ] 물체를 B 위치에 내려놓았는가?
- [ ] 에피소드 길이가 5–12초 사이인가?
- [ ] 시연 중 카메라가 가려진 구간이 없는가?
- [ ] 급격한 움직임(튀는 관절값)이 없는가?

→ 하나라도 No 이면 해당 에피소드 재수집 (`q` 로 종료 후 해당 episode 폴더 삭제)

---

## 9. 수집 전 워밍업 권장

- 스크립트 돌리기 전 **5–10회 연습 시연** (기록 없이 손으로 움직여보기)
- 일관된 경로가 몸에 익은 뒤 기록 시작
- 처음 5개 에피소드는 `meta.json` 의 `duration_s` 확인 — 5–12초 범위인지 검증

---

## 9. 레퍼런스

### ACT / 데이터 수집 관련

| 자료 | 링크 | 볼 내용 |
|------|------|---------|
| **ACT 원본 코드** | [github.com/tonyzhaozh/act](https://github.com/tonyzhaozh/act) | 시뮬 환경, HDF5 데이터 포맷, 학습 코드 |
| **ALOHA 프로젝트 페이지** | [tonyzhaozh.github.io/aloha](https://tonyzhaozh.github.io/aloha/) | 리더-팔로워 셋업 영상, 데이터 수집 방법 |
| **ACT 논문 발표 영상** | [youtube.com/watch?v=ft73x0LfGpM](https://www.youtube.com/watch?v=ft73x0LfGpM) | 전체 파이프라인 흐름 파악에 유용 |
| **원본 논문** | [arxiv.org/abs/2304.13705](https://arxiv.org/abs/2304.13705) | ACT 아키텍처 및 데이터 수집 방법 상세 |

### myCobot 280 관련

| 자료 | 링크 | 볼 내용 |
|------|------|---------|
| **mycobot_ros** | [github.com/elephantrobotics/mycobot_ros](https://github.com/elephantrobotics/mycobot_ros) | URDF 파일 (시뮬 환경 구축 시작점), ROS 연동 |

### LeRobot 관련

| 자료 | 링크 | 볼 내용 |
|------|------|---------|
| **LeRobot HuggingFace 컬렉션** | [huggingface.co/lerobot/collections](https://huggingface.co/lerobot/collections) | 전체 모델/데이터셋 목록 |
| **원본 데이터셋** | [huggingface.co/datasets/lerobot/aloha_sim_transfer_cube_human](https://huggingface.co/datasets/lerobot/aloha_sim_transfer_cube_human) | 에피소드 영상, 관절값 구조 직접 확인 가능 |
| **모델 데모 GIF** | [act_aloha_sim_transfer_cube_human/demo.gif](https://huggingface.co/lerobot/act_aloha_sim_transfer_cube_human/blob/main/demo.gif) | 학습된 모델이 어떤 동작을 하는지 |
| **데이터셋 시각화 도구** | [huggingface.co/spaces/lerobot/visualize_dataset](https://huggingface.co/spaces/lerobot/visualize_dataset) | 에피소드 영상 + 관절값 타임라인 |

### 영상 보는 법 — LeRobot 시각화 도구

1. [huggingface.co/spaces/lerobot/visualize_dataset](https://huggingface.co/spaces/lerobot/visualize_dataset) 접속
2. 입력창에 `lerobot/aloha_sim_transfer_cube_human` 입력 후 실행
3. 에피소드 번호 선택 (0–49) → 해당 에피소드 영상 + 관절값 타임라인 같이 표시됨

> 영상을 보면서 확인할 것:
> - 시연 속도 (천천히, 부드럽게)
> - top-down 카메라 앵글
> - 물체가 항상 프레임 안에 있는지
> - 동작이 완전히 끝난 뒤 기록 종료됐는지

---

### 원본 데이터셋과 우리 데이터셋 비교

| 항목 | ALOHA (원본) | myCobot 280 (우리) |
|------|-------------|-------------------|
| 에피소드 수 | 50개 | 100개 목표 |
| FPS | 50 Hz | 30 Hz |
| 관절 수 | 14 (bimanual, 양팔) | 6 (단일 팔) |
| 카메라 | top-down 1개 | top-down 1개 |
| 해상도 | 480×640 | 480×640 |

> 원본은 양팔 로봇(bimanual)이라 14-DOF — 우리 mycobot280(6-DOF)과 weight 공유 불가,  
> 아키텍처(ACT)만 동일하게 가져가고 데이터는 직접 수집해서 from scratch 학습.

---

## 10. 수집 후 빠른 검증

서버 전송 전, Raspberry Pi에서 샘플 확인:

```python
import numpy as np
ep = np.load("~/data/pickplace/episode_000000/images.npy")
print("frames:", ep.shape)          # (T, 480, 640, 3) — T가 150~360 사이여야 함 (5–12초 × 30Hz)
print("dtype:", ep.dtype)           # uint8

joints = np.load("~/data/pickplace/episode_000000/joint_states.npy")
print("joint range:", joints.min(), joints.max())   # -180 ~ 180 범위
print("joint std:", joints.std(axis=0))             # 거의 0이면 팔이 안 움직인 것
```
